# AutropicAI MCP Server — Marketing

## Install Command (GitHub)

```bash
npx github:tlkc888-Jenkins/autropicai-mcp
```

## Claude Desktop Config

```json
{
  "mcpServers": {
    "autropicai": {
      "command": "npx",
      "args": ["github:tlkc888-Jenkins/autropicai-mcp"]
    }
  }
}
```

## One-Liners

- "Find the cheapest LLM for any task — 800+ models indexed"
- "One line in your agent's config = automatic cost optimization"
- "Save 10-100x on LLM costs without changing your code"

## Where to Post

### Done
- [x] GitHub: https://github.com/tlkc888-Jenkins/autropicai-mcp

### To Do
- [ ] MCP Registry (https://registry.modelcontextprotocol.io/) — needs npm package
- [ ] r/ClaudeAI — "I made an MCP server that finds the cheapest LLM for any task"
- [ ] r/LocalLLaMA — update existing post with MCP server
- [ ] Twitter/X — "Just shipped an MCP server for LLM cost optimization"
- [ ] Discord (Claude, OpenAI, AI communities)

## Sample Posts

### Reddit (r/ClaudeAI)

**Title:** I made an MCP server that automatically picks the cheapest LLM for your tasks

**Body:**
Added one line to my Claude Desktop config and now it can look up the cheapest model for any task from 800+ options.

Install:
```json
{
  "mcpServers": {
    "autropicai": {
      "command": "npx", 
      "args": ["github:tlkc888-Jenkins/autropicai-mcp"]
    }
  }
}
```

Then ask Claude "what's the cheapest model for a simple Q&A task?" and it'll tell you.

GitHub: https://github.com/tlkc888-Jenkins/autropicai-mcp

### Twitter

🚀 Just shipped: AutropicAI MCP Server

One line in your Claude Desktop config → automatic LLM cost optimization

Your agent can now find the cheapest model from 800+ options for any task.

npx github:tlkc888-Jenkins/autropicai-mcp

#MCP #Claude #AI #LLM
